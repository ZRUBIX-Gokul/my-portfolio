"use client";

import TicketMasterDetail from "@/components/TicketMasterDetail";

export default function HousekeepingTicketsPage() {
  return <TicketMasterDetail title="House Keeping Tickets" filterDept="House Keeping" moduleId="housekeeping_tickets" />;
}
